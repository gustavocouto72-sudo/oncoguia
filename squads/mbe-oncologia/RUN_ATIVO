# FONTE ÚNICA do corpus publicado (single source of truth).
# App (app/build-data.py → backend/data/evidencia.json → /api/evidencia → Revisão
# clínica) e portão de dados (verificar_dados.py sem argumento) resolvem o run
# DAQUI — ninguém resolve "o mais novo" por conta própria.
# Formato: <run>/<versão>, relativo a squads/mbe-oncologia/output/.
# Ao promover um run novo: atualize esta linha, rode app/build-data.py e reinicie
# o backend. Runs antigos (ex.: 2026-07-21-protocolo-completo, batch rejeitado)
# ficam no disco como histórico — mas nada deve lê-los para publicar ou validar.
2026-08-18-intake-revisao/v1
