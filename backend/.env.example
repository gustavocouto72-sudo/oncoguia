# Postgres (Neon / Vercel Postgres / Supabase / local). NUNCA commitar o .env real.
DATABASE_URL=postgres://oncoguia:oncoguia123@localhost:5432/oncoguia
# Segredo do JWT — gere um forte: openssl rand -hex 32
JWT_SECRET=troque_por_secret_forte_em_producao
JWT_EXPIRY=8h
PORT=3005
# Origens liberadas no CORS (separadas por vírgula). Vazio em dev = libera tudo.
CORS_ORIGINS=
